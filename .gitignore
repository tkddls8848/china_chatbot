.env
.env*
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class

.pytest_cache/
.test-tmp/
.mypy_cache/
.ruff_cache/

data/*/*.json
data/*/*.jsonl
!data/.gitkeep
data/runtime/bot.lock
data/yfinance_cache/

*.log
*.tmp

# 영상 제작 스킬 번들(heygen-com/hyperframes). 이 저장소 코드가 아니고
# .claude/skills의 심볼릭 링크가 절대경로라 다른 기계에서 전부 깨진다.
.agents/
.claude/
skills-lock.json

# 폴리마켓 숏츠 영상 자동화. 이 저장소(주식 봇)와 의존도 배포도 공유하지 않는
# 별개 프로젝트라 추적하지 않는다. 안에 렌더 산출물(.mp4)과 썸네일이 들어 있어
# 커밋하면 코드 저장소에 바이너리가 쌓인다.
polymarket_shorts/
